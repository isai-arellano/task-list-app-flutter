import 'package:flutter/material.dart';

class ShapeCircle extends StatelessWidget {
  const ShapeCircle({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/shape.png',
                width: 141,
                height: 129,
    );
  }
}